# Attest Pilot Scorecard — five teams

Copy the per-team block below once per release candidate. One row per RC;
five teams × at least one RC each. Send back **this file only** — never
attach APKs/AABs, declaration contents, screenshots, journey media, account
references, or credentials (see `DESIGN_PARTNER_PILOT.md` §5).

Finding ids (`F-…`) and rule ids (`TG-…`/`JT-…`) are safe to share; they
reference evidence without containing it.

---

## Team 1

- Team / app (package name only):
- Release candidate (versionName/versionCode only — no binaries):
- Base build (versionName/versionCode only):
- Date run:

### Setup friction

- `attest doctor` blocking FAILs hit (list names):
- Minutes spent on setup before first `attest check`:
- What was confusing or missing in the docs:

### Time to first useful finding

- Minutes from `attest check` start to first confirmed contradiction the team agrees matters:
- First useful finding id + rule:

### Findings

- Unknown contradictions found (count of TG-001/TG-002/JT-001 the team did not already know):
- False positives (finding id + one-line reason the team disputes it):
- Total findings by state (confirmed_contradiction / changed_requires_review / evidence_missing):

### Changes made from findings

- Declaration / code / reviewer-instruction / listing edits traced to a finding id:
- Share of blocking findings acted on:

### Repeat use

- Did Attest run (local or CI) on the FOLLOWING release candidate? (yes/no):
- If yes, one-line outcome:
- Reviewer preparation time vs previous release (hours before/after):

### Buyer

- Likely buyer role (eng lead / release mgr / agency owner / other):
- Willingness to pay (yes / no / maybe + price band):

---

## Team 2

- Team / app (package name only):
- Release candidate (versionName/versionCode only — no binaries):
- Base build (versionName/versionCode only):
- Date run:

### Setup friction

- `attest doctor` blocking FAILs hit (list names):
- Minutes spent on setup before first `attest check`:
- What was confusing or missing in the docs:

### Time to first useful finding

- Minutes from `attest check` start to first confirmed contradiction the team agrees matters:
- First useful finding id + rule:

### Findings

- Unknown contradictions found (count of TG-001/TG-002/JT-001 the team did not already know):
- False positives (finding id + one-line reason the team disputes it):
- Total findings by state (confirmed_contradiction / changed_requires_review / evidence_missing):

### Changes made from findings

- Declaration / code / reviewer-instruction / listing edits traced to a finding id:
- Share of blocking findings acted on:

### Repeat use

- Did Attest run (local or CI) on the FOLLOWING release candidate? (yes/no):
- If yes, one-line outcome:
- Reviewer preparation time vs previous release (hours before/after):

### Buyer

- Likely buyer role (eng lead / release mgr / agency owner / other):
- Willingness to pay (yes / no / maybe + price band):

---

## Team 3

- Team / app (package name only):
- Release candidate (versionName/versionCode only — no binaries):
- Base build (versionName/versionCode only):
- Date run:

### Setup friction

- `attest doctor` blocking FAILs hit (list names):
- Minutes spent on setup before first `attest check`:
- What was confusing or missing in the docs:

### Time to first useful finding

- Minutes from `attest check` start to first confirmed contradiction the team agrees matters:
- First useful finding id + rule:

### Findings

- Unknown contradictions found (count of TG-001/TG-002/JT-001 the team did not already know):
- False positives (finding id + one-line reason the team disputes it):
- Total findings by state (confirmed_contradiction / changed_requires_review / evidence_missing):

### Changes made from findings

- Declaration / code / reviewer-instruction / listing edits traced to a finding id:
- Share of blocking findings acted on:

### Repeat use

- Did Attest run (local or CI) on the FOLLOWING release candidate? (yes/no):
- If yes, one-line outcome:
- Reviewer preparation time vs previous release (hours before/after):

### Buyer

- Likely buyer role (eng lead / release mgr / agency owner / other):
- Willingness to pay (yes / no / maybe + price band):

---

## Team 4

- Team / app (package name only):
- Release candidate (versionName/versionCode only — no binaries):
- Base build (versionName/versionCode only):
- Date run:

### Setup friction

- `attest doctor` blocking FAILs hit (list names):
- Minutes spent on setup before first `attest check`:
- What was confusing or missing in the docs:

### Time to first useful finding

- Minutes from `attest check` start to first confirmed contradiction the team agrees matters:
- First useful finding id + rule:

### Findings

- Unknown contradictions found (count of TG-001/TG-002/JT-001 the team did not already know):
- False positives (finding id + one-line reason the team disputes it):
- Total findings by state (confirmed_contradiction / changed_requires_review / evidence_missing):

### Changes made from findings

- Declaration / code / reviewer-instruction / listing edits traced to a finding id:
- Share of blocking findings acted on:

### Repeat use

- Did Attest run (local or CI) on the FOLLOWING release candidate? (yes/no):
- If yes, one-line outcome:
- Reviewer preparation time vs previous release (hours before/after):

### Buyer

- Likely buyer role (eng lead / release mgr / agency owner / other):
- Willingness to pay (yes / no / maybe + price band):

---

## Team 5

- Team / app (package name only):
- Release candidate (versionName/versionCode only — no binaries):
- Base build (versionName/versionCode only):
- Date run:

### Setup friction

- `attest doctor` blocking FAILs hit (list names):
- Minutes spent on setup before first `attest check`:
- What was confusing or missing in the docs:

### Time to first useful finding

- Minutes from `attest check` start to first confirmed contradiction the team agrees matters:
- First useful finding id + rule:

### Findings

- Unknown contradictions found (count of TG-001/TG-002/JT-001 the team did not already know):
- False positives (finding id + one-line reason the team disputes it):
- Total findings by state (confirmed_contradiction / changed_requires_review / evidence_missing):

### Changes made from findings

- Declaration / code / reviewer-instruction / listing edits traced to a finding id:
- Share of blocking findings acted on:

### Repeat use

- Did Attest run (local or CI) on the FOLLOWING release candidate? (yes/no):
- If yes, one-line outcome:
- Reviewer preparation time vs previous release (hours before/after):

### Buyer

- Likely buyer role (eng lead / release mgr / agency owner / other):
- Willingness to pay (yes / no / maybe + price band):

---

## Pilot totals (fill after all five)

- Teams with ≥ 1 unknown contradiction:
- Median time to first useful finding:
- Share of blocking findings acted on (across teams):
- Teams that ran Attest on the next RC (target ≥ 2):
- Teams that named a buyer (target ≥ 3):
- Continue / reposition per `DESIGN_PARTNER_PILOT.md` §6 stop criteria:
